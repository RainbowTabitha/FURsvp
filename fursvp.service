[Unit]
Description=fursvp daemon
After=network.target

[Service]
User={username}
Group={username}
WorkingDirectory=/home/{username}/FURsvp/
ExecStart=/usr/bin/gunicorn fursvp.wsgi:application
Restart=always

[Install]
WantedBy=multi-user.target
